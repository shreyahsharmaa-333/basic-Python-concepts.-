Table Setup
CREATE TABLE Orders (
    OrderID INT PRIMARY KEY,
    OrderDate DATE,
    Amount DECIMAL(10, 2)
);

INSERT INTO Orders (OrderID, OrderDate, Amount)
VALUES
(1, '2024-11-01', 250.50),
(2, '2024-11-02', 300.75),
(3, '2024-11-03', 150.25);

✅ Queries
1. Convert the Amount column to a string
SELECT OrderID, CAST(Amount AS CHAR) AS AmountAsString
FROM Orders;

2. Convert the OrderDate column to a string format


SELECT OrderID, DATE_FORMAT(OrderDate, '%Y-%m-%d') AS OrderDateAsString
FROM Orders;


%Y → 4-digit year

%m → month (01–12)

%d → day (01–31)

3. Convert a string '2024-11-05' into a date format
SELECT STR_TO_DATE('2024-11-05', '%Y-%m-%d') AS ConvertedDate;

4. Extract the year from OrderDate
SELECT OrderID, YEAR(OrderDate) AS OrderYear
FROM Orders;

5. Concatenate OrderDate and Amount into a single string
SELECT OrderID,
       CONCAT(OrderDate, ' - ', Amount) AS DateAmount
FROM Orders;
